# This file is @generated by <https://github.com/liblaf/copier-python>.
# DO NOT EDIT!

name: Bench

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  skip-duplicate:
    name: Skip Duplicate
    permissions:
      actions: write
    runs-on: ubuntu-latest
    outputs:
      should-skip: ${{ steps.skip-duplicate.outputs.should-skip }}
    steps:
      - id: skip-duplicate
        name: Skip Duplicate
        uses: liblaf/actions/skip-duplicate@f6cf271d2b7400fb019d0366c1a389ab23239730 # dist
        with:
          cancel-others: true

  collect:
    name: Collect
    needs:
      - skip-duplicate
    if: needs.skip-duplicate.outputs.should-skip != 'true'
    runs-on: ubuntu-latest
    outputs:
      has-benches: ${{ steps.collect.outputs.has-benches }}
    steps:
      - name: Checkout
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
      - name: Setup Python
        uses: liblaf/actions/setup-python@f6cf271d2b7400fb019d0366c1a389ab23239730 # dist
      - id: collect
        name: Collect
        run: |-
          if pytest -m 'benchmark' --collect-only; then
            echo 'has-benches=true' >> "$GITHUB_OUTPUT"
          else
            echo 'has-benches=false' >> "$GITHUB_OUTPUT"
          fi

  bench:
    name: Bench
    needs:
      - collect
    if: needs.collect.outputs.has-benches == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
      - name: Setup Python
        uses: liblaf/actions/setup-python@f6cf271d2b7400fb019d0366c1a389ab23239730 # dist
      - name: Install Mise
        uses: jdx/mise-action@5228313ee0372e111a38da051671ca30fc5a96db # v3
      - name: Bench
        uses: CodSpeedHQ/action@f22792bfac16f3e14eb9fbea76f4a48e9cc22b93 # v4
        with:
          run: mise run bench
          mode: instrumentation
        env:
          FORCE_COLOR: 1
